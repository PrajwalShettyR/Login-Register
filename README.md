Login / Register Authentication System

A simple authentication system with user registration and login functionality built using React (Frontend) and Flask (Backend). The application allows users to create an account and log in using their credentials.
